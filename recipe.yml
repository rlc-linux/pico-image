name: RusLand Pico
id: pico
vibversion: 1.0.0

stages:
- id: build
  base: scratch
  singlelayer: true
  labels:
    maintainer: Ruslan Artemev
  adds:
    - srcdst:
        rootfs/vanilla-pico.tar.zst: /
  cmd:
    exec:
      - /bash
